# 🔴 CRITICAL BUGFIX PLAN V3 - Реальні проблеми виявлені

**Дата:** 06.11.2025  
**Версія:** 3.0 - Після повного аналізу коду  
**Статус:** 🔴 КРИТИЧНИЙ

---

## 🎯 ROOT CAUSES - Знайдено ВСІ проблеми

### **ПРОБЛЕМА #1: AttributeError 'NoneType' has no attribute 'empty'**

**Локація:** `gui/ui_manager.py:set_ui_busy()` лінія ~290

**Код з помилкою:**
```python
def set_ui_busy(self, is_busy):
    self.mw.run_analysis_button.setEnabled(not is_busy)
    is_data_loaded = not self.mw.analysis_results_df.empty  # ❌ ПОМИЛКА ТУТ!
    # Коли analysis_results_df = None → викликається .empty на None
    self.mw.packing_list_button.setEnabled(not is_busy and is_data_loaded)
    # ...
```

**Чому виникає:**
- При зміні клієнта викликається `load_client_config()`
- Там встановлюється `self.analysis_results_df = None`
- Потім викликається `_update_all_views()`
- Який викликає `self.ui_manager.set_ui_busy(False)`
- І тут відбувається `.empty` на None

---

### **ПРОБЛЕМА #2: Run Analysis кнопка не активується**

**Локація:** `gui/file_handler.py:validate_file()` + `select_orders_file()`/`select_stock_file()`

**Проблема:**
```python
def select_orders_file(self):
    # Завантажує файл
    self.mw.orders_file_path = file_path
    # ❌ НЕ ВИКЛИКАЄ validate_file()!
    # Тому orders_file_status_label залишається пустим
    
def check_files_ready(self):
    orders_ok = self.mw.orders_file_path and self.mw.orders_file_status_label.text() == "✓"
    # ❌ Статус завжди пустий, тому orders_ok = False
```

**Що відбувається:**
1. Користувач завантажує orders.csv та stock.csv
2. Файли зберігаються в `orders_file_path` та `stock_file_path`
3. АЛЕ `validate_file()` не викликається автоматично
4. Тому status labels залишаються пустими
5. `check_files_ready()` повертає False
6. Кнопка Run Analysis залишається disabled

---

### **ПРОБЛЕМА #3: Client Settings не реагує**

**Локація:** `gui/actions_handler.py` - метод **НЕ ІСНУЄ**!

**Проблема:**
```python
# В gui/main_window_pyside.py:connect_signals()
self.settings_button.clicked.connect(self.actions_handler.open_settings_window)

# АЛЕ в gui/actions_handler.py:
class ActionsHandler(QObject):
    def __init__(self, main_window):
        # ...
    
    def create_new_session(self):
        # ...
    
    def run_analysis(self):
        # ...
    
    # ❌ МЕТОД open_settings_window() НЕ ІСНУЄ!
```

**Що відбувається:**
1. Користувач натискає кнопку "Client Settings"
2. Qt намагається викликати `actions_handler.open_settings_window()`
3. Метод не існує → нічого не відбувається
4. Помилка НЕ виводиться в логи (Qt просто ігнорує)

---

## 🚀 ПРОМПТИ ДЛЯ CLAUDE CODE

### PROMPT 1: Fix ui_manager.py - AttributeError

```
Репозиторій: shopify-fulfillment-tool

КРИТИЧНА ПОМИЛКА: AttributeError 'NoneType' has no attribute 'empty' при перемиканні клієнтів

Файл для виправлення: gui/ui_manager.py

Метод: set_ui_busy() (приблизно лінія 290)

ПРОБЛЕМА:
```python
def set_ui_busy(self, is_busy):
    self.mw.run_analysis_button.setEnabled(not is_busy)
    is_data_loaded = not self.mw.analysis_results_df.empty  # ❌ БУГ ТУТ
    self.mw.packing_list_button.setEnabled(not is_busy and is_data_loaded)
    # ...
```

РІШЕННЯ - Додати перевірку на None:
```python
def set_ui_busy(self, is_busy):
    """Enables or disables UI elements based on application state.
    
    Args:
        is_busy (bool): If True, disables interactive widgets
    """
    self.mw.run_analysis_button.setEnabled(not is_busy)
    
    # ВИПРАВЛЕННЯ: Перевірити що DataFrame не None перед викликом .empty
    is_data_loaded = (
        self.mw.analysis_results_df is not None 
        and not self.mw.analysis_results_df.empty
    )
    
    self.mw.packing_list_button.setEnabled(not is_busy and is_data_loaded)
    self.mw.stock_export_button.setEnabled(not is_busy and is_data_loaded)
    self.mw.report_builder_button.setEnabled(not is_busy and is_data_loaded)
    
    self.log.debug(f"UI busy state set to: {is_busy}, data_loaded: {is_data_loaded}")
```

ТЕСТУВАННЯ:
1. Запустити додаток
2. Обрати клієнта TEST
3. Перемкнути на клієнта RPTN
4. Помилка НЕ має виникнути
5. Перевірити що кнопки правильно активуються/деактивуються
```

---

### PROMPT 2: Fix file_handler.py - Run Analysis не активується

```
Репозиторій: shopify-fulfillment-tool

КРИТИЧНА ПОМИЛКА: Кнопка Run Analysis не активується після завантаження файлів

Файл для виправлення: gui/file_handler.py

ПРОБЛЕМА: Валідація файлів не викликається після завантаження

Методи для виправлення:
1. select_orders_file()
2. select_stock_file()

ПОТОЧНИЙ КОД (НЕ ПРАЦЮЄ):
```python
def select_orders_file(self):
    file_path, _ = QFileDialog.getOpenFileName(...)
    if file_path:
        self.mw.orders_file_path = file_path
        self.mw.orders_file_path_label.setText(file_path)
        # ❌ НЕ викликає validate_file()
```

РІШЕННЯ - Додати виклик валідації:
```python
def select_orders_file(self):
    """Opens file dialog for orders CSV and validates it."""
    file_path, _ = QFileDialog.getOpenFileName(
        self.mw,
        "Select Orders File",
        "",
        "CSV Files (*.csv);;All Files (*)"
    )
    
    if file_path:
        self.log.info(f"Orders file selected: {file_path}")
        self.mw.orders_file_path = file_path
        self.mw.orders_file_path_label.setText(os.path.basename(file_path))
        
        # ВИПРАВЛЕННЯ: Викликати валідацію
        self.validate_file("orders")
        
        # Check if both files are ready
        self.check_files_ready()

def select_stock_file(self):
    """Opens file dialog for stock CSV and validates it."""
    file_path, _ = QFileDialog.getOpenFileName(
        self.mw,
        "Select Stock File",
        "",
        "CSV Files (*.csv);;All Files (*)"
    )
    
    if file_path:
        self.log.info(f"Stock file selected: {file_path}")
        self.mw.stock_file_path = file_path
        self.mw.stock_file_path_label.setText(os.path.basename(file_path))
        
        # ВИПРАВЛЕННЯ: Викликати валідацію
        self.validate_file("stock")
        
        # Check if both files are ready
        self.check_files_ready()
```

ТАКОЖ ВИПРАВИТИ validate_file():
```python
def validate_file(self, file_type):
    """Validates CSV file using client-specific configuration."""
    
    # Get client config
    if not self.mw.current_client_id or not self.mw.current_client_config:
        self.log.warning("No client selected or config not loaded")
        return
    
    client_config = self.mw.current_client_config
    
    if file_type == "orders":
        path = self.mw.orders_file_path
        label = self.mw.orders_file_status_label
        required_cols = client_config.get("column_mappings", {}).get("orders_required", [])
        delimiter = ","
    else:  # stock
        path = self.mw.stock_file_path
        label = self.mw.stock_file_status_label
        required_cols = client_config.get("column_mappings", {}).get("stock_required", [])
        delimiter = client_config.get("settings", {}).get("stock_csv_delimiter", ";")
    
    if not path:
        return
    
    try:
        # Validate columns
        is_valid = core.validate_csv_headers(path, required_cols, delimiter)
        
        if is_valid:
            label.setText("✓")
            label.setStyleSheet("color: green; font-weight: bold;")
            self.log.info(f"{file_type.capitalize()} file validated successfully")
        else:
            label.setText("✗")
            label.setStyleSheet("color: red; font-weight: bold;")
            self.log.warning(f"{file_type.capitalize()} file validation failed")
            
    except Exception as e:
        label.setText("✗")
        label.setStyleSheet("color: red;")
        self.log.error(f"Error validating {file_type} file: {e}")
```

ТЕСТУВАННЯ:
1. Запустити додаток
2. Обрати клієнта TEST
3. Створити нову сесію
4. Завантажити orders.csv → має з'явитись ✓ або ✗
5. Завантажити stock.csv → має з'явитись ✓ або ✗
6. Якщо обидва ✓ → кнопка Run Analysis має активуватись
```

---

### PROMPT 3: Create open_settings_window() method

```
Репозиторій: shopify-fulfillment-tool

КРИТИЧНА ПОМИЛКА: Кнопка Client Settings не працює, метод не існує

Файл для створення методу: gui/actions_handler.py

ПРОБЛЕМА: Метод open_settings_window() відсутній в класі ActionsHandler

РІШЕННЯ - Створити метод:
```python
def open_settings_window(self):
    """Opens the settings window for the active client.
    
    Allows editing client-specific configuration like:
    - Column mappings
    - Filters for packing lists and stock exports
    - Courier mappings
    - Rules and tags
    """
    self.log.info("Opening client settings...")
    
    # Check if client is selected
    if not self.mw.current_client_id:
        QMessageBox.warning(
            self.mw,
            "No Client Selected",
            "Please select a client before opening settings."
        )
        return
    
    # Load current client config
    try:
        client_config = self.mw.profile_manager.load_shopify_config(
            self.mw.current_client_id
        )
        
        if not client_config:
            raise Exception("Failed to load client configuration")
        
        self.log.info(f"Loaded config for CLIENT_{self.mw.current_client_id}")
        
    except Exception as e:
        self.log.error(f"Failed to load client config: {e}", exc_info=True)
        QMessageBox.critical(
            self.mw,
            "Error",
            f"Failed to load client configuration:\n\n{str(e)}"
        )
        return
    
    # Open settings dialog
    from gui.settings_window_pyside import SettingsWindow
    
    try:
        settings_win = SettingsWindow(
            client_id=self.mw.current_client_id,
            client_config=client_config,
            profile_manager=self.mw.profile_manager,
            parent=self.mw
        )
        
        # Show dialog and wait for result
        if settings_win.exec():
            # Settings were saved, reload config
            self.log.info("Settings dialog accepted, reloading config...")
            
            try:
                self.mw.current_client_config = self.mw.profile_manager.load_shopify_config(
                    self.mw.current_client_id
                )
                
                self.log.info(f"Config reloaded for CLIENT_{self.mw.current_client_id}")
                
                QMessageBox.information(
                    self.mw,
                    "Settings Saved",
                    "Client settings have been saved successfully."
                )
                
            except Exception as e:
                self.log.error(f"Failed to reload config: {e}", exc_info=True)
                QMessageBox.warning(
                    self.mw,
                    "Warning",
                    f"Settings were saved but failed to reload:\n\n{str(e)}"
                )
        else:
            self.log.info("Settings dialog cancelled")
            
    except Exception as e:
        self.log.error(f"Error opening settings window: {e}", exc_info=True)
        QMessageBox.critical(
            self.mw,
            "Error",
            f"Failed to open settings window:\n\n{str(e)}"
        )
```

ТАКОЖ ПОТРІБНО ОНОВИТИ SettingsWindow constructor:
```python
# В gui/settings_window_pyside.py

class SettingsWindow(QDialog):
    def __init__(self, client_id: str, client_config: dict, profile_manager, parent=None):
        """Initialize settings window.
        
        Args:
            client_id: Client ID (e.g., "TEST", "RPTN")
            client_config: Client-specific Shopify configuration
            profile_manager: ProfileManager instance for saving
            parent: Parent widget
        """
        super().__init__(parent)
        
        self.client_id = client_id
        self.client_config = client_config.copy()  # Work with copy
        self.profile_manager = profile_manager
        
        self.setWindowTitle(f"Settings - CLIENT_{client_id}")
        self.setMinimumWidth(800)
        self.setMinimumHeight(600)
        
        self._init_ui()
        self._load_config_to_ui()
    
    def save_settings(self):
        """Save settings back to server."""
        try:
            # Update config dict from UI
            self._update_config_from_ui()
            
            # Save to server via ProfileManager
            success = self.profile_manager.save_shopify_config(
                self.client_id,
                self.client_config
            )
            
            if success:
                self.accept()  # Close dialog with OK
            else:
                QMessageBox.critical(
                    self,
                    "Error",
                    "Failed to save settings to server."
                )
                
        except Exception as e:
            QMessageBox.critical(
                self,
                "Error",
                f"Failed to save settings:\n\n{str(e)}"
            )
```

ТЕСТУВАННЯ:
1. Запустити додаток
2. Обрати клієнта TEST
3. Натиснути кнопку "Open Client Settings"
4. Вікно настройок має відкритись
5. Змінити якісь налаштування
6. Натиснути Save
7. Перевірити що конфіг зберігся на сервері
```

---

## ⏱️ ОЦІНКА ЧАСУ

**PROMPT 1:** 15 хв (1 файл, проста зміна)  
**PROMPT 2:** 30 хв (2 методи, валідація)  
**PROMPT 3:** 45 хв (новий метод + оновлення SettingsWindow)

**Загальний час:** ~1.5 години

---

## ✅ КРИТЕРІЇ УСПІХУ

### Після PROMPT 1:
- ✅ Перемикання між клієнтами працює без помилок
- ✅ Помилка AttributeError більше не виникає
- ✅ Кнопки правильно активуються/деактивуються

### Після PROMPT 2:
- ✅ Завантаження файлів викликає валідацію
- ✅ Status labels показують ✓ або ✗
- ✅ Кнопка Run Analysis активується після завантаження обох файлів
- ✅ Аналіз запускається без проблем

### Після PROMPT 3:
- ✅ Кнопка Client Settings відкриває вікно
- ✅ Можна редагувати налаштування клієнта
- ✅ Зміни зберігаються на сервер
- ✅ Конфіг перезавантажується після збереження

---

## 📝 ПОРЯДОК ВИКОНАННЯ

1. **СПОЧАТКУ PROMPT 1** - виправити AttributeError (найкритичніше)
2. **ПОТІМ PROMPT 2** - виправити валідацію файлів
3. **НАОСТАНОК PROMPT 3** - додати Client Settings

Після кожного промпту - тестувати перед переходом до наступного!

---

## 🔄 НАСТУПНІ КРОКИ

Після виправлення цих 3 критичних проблем:
- Bug #2 (відкриття сесій) - PHASE 1
- Bug #4 & #5 (Packing Lists, Stock Exports) - PHASE 2
- Bug #6 & #7 (Stats, History) - PHASE 3

Але спочатку виправити ці 3 - вони блокують базовий функціонал!
